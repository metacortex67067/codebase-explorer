# Web framework
# fastapi>=0.115.4 is required so its starlette pin (>=0.40) is compatible
# with mcp 1.0.0, which needs starlette>=0.39. The old fastapi==0.115.0
# pinned starlette<0.39 and conflicted with mcp.
fastapi>=0.115.4,<0.116
uvicorn[standard]>=0.32,<0.35
python-multipart>=0.0.12
pydantic>=2.9,<3
pydantic-settings>=2.6,<3

# LLM
# OpenAI SDK drives both the local Ollama endpoint and OpenAI-compatible
# clouds (Groq, OpenRouter). anthropic is optional (only for the paid path).
openai>=1.30,<3
anthropic>=0.39  # optional: only needed when LLM_PROVIDER=anthropic

# Embeddings + vector store
sentence-transformers>=3.2,<4
chromadb>=0.5.15,<0.6
trio>=0.27  # pulled in by chromadb's async resolver in this environment

# Git operations
gitpython>=3.1.43

# MCP server
mcp==1.0.0

# Testing
pytest>=8.3
pytest-cov>=5
pytest-asyncio>=0.24
httpx>=0.27,<0.28
